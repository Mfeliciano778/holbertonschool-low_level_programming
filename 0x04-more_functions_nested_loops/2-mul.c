#include "holberton.h"
#include <stdio.h>
/**
 * mul - multiply two integers
 * @num1: first integer we are multiplying
 * @num2: second integer we are multiplying
 *
 * Return: Always 0.
 */
int mul(int num1, int num2)
{
	return (num1 * num2);
}
