#include <stdlib.h>
#include <stdio.h>
#include "holberton.h"
/**
 * main - writes the alphabet in lowercase
 *
 *
 * Return: 0
 *
 */
void print_alphabet(void)
{
	int c;

	for 
	for (c = 'a', c <= 'z', c++)
	{
		_putchar(c);
	}
	_putchar('\n');

	return (0);
}
